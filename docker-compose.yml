version: "2.2"
   
services:
  db:
    image: mysql:8
    environment:
      - MYSQL_DB=mysql
      - MYSQL_USER=mysql
      - MYSQL_PASSWORD=mysql
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    depends_on:
      - db
